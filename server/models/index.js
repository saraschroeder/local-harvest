const User = require('./User')
const Review = require('./Review')
const Post = require('./Post')

module.exports = { User, Review, Post }

// Ask team, should we only export user and have Review and Post as arrays inside of each other??
